diff --git a/client/src/Class88.java b/client/src/Class88.java
--- a/client/src/Class88.java
+++ b/client/src/Class88.java
@@ -67,6 +67,14 @@ final class Class88 {
         } finally {
             if (interfaceScaled) {
                 Applet_Sub1.endInterfaceRenderScale();
+
+                // The resizable 634 viewport is continuously rendered and can
+                // overlap transparent HUD/chat widgets. With native UI scaling,
+                // keep those lightweight interface regions dirty so they are
+                // composited again on the next frame instead of being gradually
+                // overwritten by the scene after a resize/sidebar transition.
+                for (int dirtyIndex = 0; dirtyIndex < Class152.aBooleanArray2076.length; dirtyIndex++)
+                    Class152.aBooleanArray2076[dirtyIndex] = true;
             }
         }
     }
